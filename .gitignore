__pycache__/
*.py[cod]
.venv/
venv/

.env

.pytest_cache/
.ruff_cache/
.mypy_cache/

.idea/
.vscode/

# Playwright MCP's own screenshot/console-log scratch output, not project
# source
.playwright-mcp/

# User-uploaded content -- not source, shouldn't be committed
static/avatars/
static/reference-articles/
static/team-logos/

# Live edge-nginx config, swapped by the deploy runbook between
# deploy/nginx/bootstrap.conf and deploy/nginx/app.conf -- see
# docs/deploy.md. Not a template, shouldn't be committed.
deploy/nginx/active.conf
.env.prod
