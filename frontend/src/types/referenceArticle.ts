export interface ReferenceArticleSummary {
  id: string
  title: string
  category: string
}

export interface ReferenceArticleDetail {
  id: string
  title: string
  category: string
  body: string
  created_at: string
}
